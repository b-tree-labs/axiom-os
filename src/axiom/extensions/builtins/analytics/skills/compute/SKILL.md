---
name: analytics.compute
description: Deterministic math over a numeric series (descriptive stats, regression/fit,
  calculus on samples). Call this instead of computing an average/total/slope/integral
  yourself; the result is provenance-stamped.
version: 0.1.0
inputs:
- name: op
  type: sum|mean|min|max|std|var|median|percentile|range|count|linregress|polyfit|moving_average|rate_of_change|correlation|derivative|integral|cumsum|interpolate
- name: series
  type: list of numbers, list of row-objects, or JSON/CSV/table text
- name: column
  type: column/key to use when series is rows (optional)
- name: params
  type: op params, e.g. {window:7}, {degree:2}, {q:95} (optional)
- name: source
  type: 'provenance: the tool/series the data came from (optional)'
outputs:
- kind: SkillResult
allowed-tools: []
generator: axi-skills-emit-md
---

Skill ``analytics.compute`` — see registration in the providing extension's ``skills/__init__.py``. This SKILL.md is generated by ``axi skills emit-md`` (ADR-063); edit the Python ``SkillSpec`` to change it.
