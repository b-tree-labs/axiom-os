---
name: data.ensure_schema
description: 'Bring the conformance tier''s schema up to the installed code — the
  silver.signals columns and the gold views. The Alembic-versioned extensions are
  `axi db migrate upgrade head`; this is the part that is raw DDL and was only ever
  applied by a conform pass, which is how a release can install cleanly and leave
  the database behind it. Reports the columns it actually added, read back after the
  fact. Skips index creation: that blocks writes and wants a maintenance window.'
version: 0.3.0
inputs:
- name: dsn
  type: str
- name: lock_timeout
  type: str
outputs:
- kind: SkillResult
allowed-tools: []
generator: axi-skills-emit-md
---

Skill ``data.ensure_schema`` — see registration in the providing extension's ``skills/__init__.py``. This SKILL.md is generated by ``axi skills emit-md`` (ADR-063); edit the Python ``SkillSpec`` to change it.
