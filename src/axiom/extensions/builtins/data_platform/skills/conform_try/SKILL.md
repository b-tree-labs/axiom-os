---
name: data.conform_try
description: Dry-run a registered normalizer over one bronze record and report the
  canonical rows it yields, plus the mistakes silver would absorb silently — a duplicate
  channel, a naive timestamp, a missing unit. Writes nothing. Omit schema_ref to list
  what is registered.
version: 0.3.0
inputs:
- name: schema_ref
  type: str
- name: record
  type: object
outputs:
- kind: SkillResult
allowed-tools: []
generator: axi-skills-emit-md
---

Skill ``data.conform_try`` — see registration in the providing extension's ``skills/__init__.py``. This SKILL.md is generated by ``axi skills emit-md`` (ADR-063); edit the Python ``SkillSpec`` to change it.
