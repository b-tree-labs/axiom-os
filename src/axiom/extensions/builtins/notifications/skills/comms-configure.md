# comms.configure
Guided communications-config (ADR-074): AXI asks the right questions given available channels, then assembles a scenario-conditioned ChannelPolicy (preferred channels by status/topic/time). NL-drivable.
