{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DiagnosticsConfig",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "default_severity": {
      "type": "string",
      "enum": ["info", "warn", "error"],
      "default": "warn",
      "description": "Severity threshold below which doctor checks are suppressed in non-verbose runs.",
      "x-reloadable": true
    },
    "auto_fix_enabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether `axi doctor --auto-fix` is allowed to mutate state without explicit confirmation.",
      "x-reloadable": false
    },
    "max_parallel_checks": {
      "type": "integer",
      "minimum": 1,
      "maximum": 32,
      "default": 4,
      "description": "Upper bound on concurrent health checks the doctor agent runs.",
      "x-reloadable": true
    }
  }
}
